---
import BaseLayout from '../layouts/BaseLayout.astro';
import { postReadTime } from '../lib/readTimes';

const BATCH = 20;

interface Post {
  href: string;
  badge?: string;
  tags: { text: string; cls: string }[];
  title: string;
  desc: string;
  date: string;
  read: string;
}

const latestPosts: Post[] = [
  {
    href: '/posts/dmoe-block-moe/',
    badge: '★ Latest',
    tags: [
      { text: 'Diffusion LLMs', cls: 'tag-purple' },
      { text: 'Mixture-of-Experts', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'dMoE: Block-Level Expert Routing for Diffusion LLMs',
    desc: 'Diffusion LLMs decode blocks of tokens in parallel, but MoE layers route experts per token — loading 70+ unique weights each forward pass. dMoE aggregates token scores into a block-level distribution, selects a top-p coreset, and cuts unique expert loads by 80% while retaining 99.1% of performance.',
    date: 'June 8, 2026',
    read: postReadTime('dmoe-block-moe'),
  },
  {
    href: '/posts/mimo-dflash-ultraspeed/',
    tags: [
      { text: 'Speculative Decoding', cls: 'tag-purple' },
      { text: 'Diffusion LMs', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'MiMo 2.5 Pro UltraSpeed: 1000 Tokens/Second on a Trillion-Parameter Model',
    desc: 'How Xiaomi combined DFlash block-diffusion speculative decoding, MXFP4 expert quantization, and TileRT persistent-engine kernels to break 1000 tokens/second on a 1-trillion-parameter MoE model — using commodity 8-GPU hardware.',
    date: 'June 8, 2026',
    read: postReadTime('mimo-dflash-ultraspeed'),
  },
  {
    href: '/posts/cola-depth-adaptation/',
    tags: [
      { text: 'Depth Adaptation', cls: 'tag-purple' },
      { text: 'LLM Reasoning', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Skip a Layer or Loop it? Test-Time Depth Adaptation of LLMs',
    desc: 'CoLa treats pretrained transformer layers as recomposable modules — skipping redundant ones and looping key ones — using MCTS to find the optimal execution path per input. No training required. LLaMA-3-8B-Instruct improves from 6% to 66% on hard math reasoning, often with fewer total layer invocations.',
    date: 'June 8, 2026',
    read: postReadTime('cola-depth-adaptation'),
  },
  {
    href: '/posts/opd-horizon-control/',
    tags: [
      { text: 'On-policy Distillation', cls: 'tag-purple' },
      { text: 'LLM Reasoning', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Are Full Rollouts Necessary for On-Policy Distillation?',
    desc: 'Controlling the rollout horizon improves the efficiency of on-policy distillation for LLM reasoning, reducing training time by up to 82% without sacrificing performance.',
    date: 'June 6, 2026',
    read: postReadTime('opd-horizon-control'),
  },
  {
    href: '/posts/latent-space/',
    tags: [
      { text: 'Latent Space', cls: 'tag-purple' },
      { text: 'Survey', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'The Latent Space: The Continuous Future of Language-Based Models',
    desc: 'Is explicit text decoding holding AI back? We deep-dive into the structural mechanics, representations, and capabilities of continuous latent-space computation as a machine-native substrate for reasoning, memory, and collaboration.',
    date: 'June 6, 2026',
    read: postReadTime('latent-space'),
  },
  {
    href: '/posts/representation-forcing/',
    tags: [
      { text: 'Multimodal Generation', cls: 'tag-purple' },
      { text: 'Pixel Space', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Representation Forcing: Bottleneck-Free Unified Multimodal Models',
    desc: 'Unified multimodal models typically rely on separately pretrained VAEs, imposing a lossy bottleneck. Representation Forcing eliminates the VAE entirely by training the decoder to autoregressively predict visual representations from the model’s jointly trained vision encoder. This simple mechanism closes the pixel-space quality gap while boosting understanding.',
    date: 'June 6, 2026',
    read: postReadTime('representation-forcing'),
  },
  {
    href: '/posts/muon-curvature/',
    tags: [
      { text: 'Optimization', cls: 'tag-purple' },
      { text: 'Curvature Analysis', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Why Muon Outperforms Adam: A Curvature Perspective',
    desc: 'Why does the spectrally normalized Muon optimizer train LLMs 2× faster than Adam? Using a second-order Taylor expansion of the loss landscape, we show that Muon reduces Normalized Directional Sharpness (NDS) rather than changing update scale, particularly in the presence of imbalanced pretraining data.',
    date: 'June 6, 2026',
    read: postReadTime('muon-curvature'),
  },
  {
    href: '/posts/oprd/',
    tags: [
      { text: 'Representation Distillation', cls: 'tag-purple' },
      { text: 'On-Policy Distillation', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'OPRD: On-Policy Representation Distillation',
    desc: 'Output-space On-Policy Distillation (OPD) dominates language model post-training but suffers from high Monte Carlo sampling variance and vocabulary compression bottlenecks. OPRD moves supervision into the hidden-state space, achieving deterministic gradient updates, richer per-layer structural alignment, 1.44× faster training, and 54% peak memory cuts.',
    date: 'June 6, 2026',
    read: postReadTime('oprd'),
  },
  {
    href: '/posts/muon-optimizer/',
    tags: [
      { text: 'Optimization', cls: 'tag-purple' },
      { text: 'Scaling Laws', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Muon: Scaling Matrix Orthogonalization for LLM Training',
    desc: 'Standard optimizers treat parameters as flat vectors, losing their matrix geometry and causing singular value skew. Muon solves this by orthogonalizing gradient momentum updates using Newton-Schulz iteration, yielding a 2× training efficiency boost.',
    date: 'June 6, 2026',
    read: postReadTime('muon-optimizer'),
  },
  {
    href: '/posts/draft-opd/',
    tags: [
      { text: 'Speculative Decoding', cls: 'tag-purple' },
      { text: 'On-Policy Distillation', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Draft-OPD: On-Policy Distillation for Speculative Draft Models',
    desc: 'Supervised fine-tuning of speculative draft models plateaus due to offline-to-inference mismatch. Draft-OPD uses anchored rollouts, error-position replay, and asymmetric acceptance-aware distillation losses to post-train drafters on verification-time errors, yielding 5x+ speedups.',
    date: 'June 5, 2026',
    read: postReadTime('draft-opd'),
  },
  {
    href: '/posts/tropd/',
    tags: [
      { text: 'On-Policy Distillation', cls: 'tag-purple' },
      { text: 'Reasoning Models', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'TrOPD: Trust Region On-Policy Distillation for Stable SRM Training',
    desc: 'On-Policy Distillation (OPD) for reasoning models suffers from training instability and policy-gradient outliers. TrOPD stabilizes optimization using a speculative agreement-ratio trust region, top-k forward KL outlier estimation, and annealed off-policy guidance.',
    date: 'June 4, 2026',
    read: postReadTime('tropd'),
  },
  {
    href: '/posts/gemini-embedding-2/',
    tags: [
      { text: 'Multimodal', cls: 'tag-purple' },
      { text: 'Embeddings', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Gemini Embedding 2: A Native Multimodal Embedding Model',
    desc: 'Traditionally, multimodal retrieval relies on late fusion or speech-to-text cascades that introduce transcription bottlenecks. Gemini Embedding 2 solves this by natively processing interleaved video, audio, image, and text in a unified continuous semantic space.',
    date: 'June 4, 2026',
    read: postReadTime('gemini-embedding-2'),
  },
  {
    href: '/posts/gemma4-12b-explained/',
    tags: [
      { text: 'Gemma 4', cls: 'tag-purple' },
      { text: 'Multimodal', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Gemma 4 12B Explained: The Encoder-Free Multimodal Revolution',
    desc: "A technical deep dive into Google DeepMind's Gemma 4 12B: how it drops separate vision and audio encoders entirely, directly projects raw pixel patches and acoustic frames, and uses shared-KV speculative MTP assistant for local execution.",
    date: 'June 4, 2026',
    read: postReadTime('gemma4-12b-explained'),
  },
  {
    href: '/posts/simsd/',
    tags: [
      { text: 'Speculative Decoding', cls: 'tag-purple' },
      { text: 'Diffusion LMs', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'SimSD: Restoring Token-Level Speculative Decoding for Diffusion Language Models',
    desc: 'Diffusion language models generate sequences in parallel using bidirectional attention, making them incompatible with standard speculative decoding. SimSD bridges this gap with a training-free attention mask and data-mask duplication layout that evaluates valid target logits for multiple draft tokens in a single forward pass, unlocking a 7.46× speedup.',
    date: 'June 3, 2026',
    read: postReadTime('simsd'),
  },
  {
    href: '/posts/drafter-on-policy-distillation/',
    tags: [
      { text: 'Speculative Decoding', cls: 'tag-purple' },
      { text: 'On-Policy Distillation', cls: 'tag-cyan' },
      { text: 'Drafter Training', cls: 'tag-amber' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Should We Train Speculative Drafters On-Policy?',
    desc: "Drafters are usually trained offline on teacher-forced data and treated as an attachment to the main model. Should we instead train them like a distilled student — on their own self-generated drafts? An interactive synthesis of HASS, DistillSpec, EAGLE-3, and Online Speculative Decoding showing why the frontier already says yes — and why the lossless verifier means on-policy training buys pure speed, never quality.",
    date: 'June 3, 2026',
    read: postReadTime('drafter-on-policy-distillation'),
  },
  {
    href: '/posts/why-larger-models-learn-more/',
    tags: [
      { text: 'Model Scale', cls: 'tag-purple' },
      { text: 'Learning Dynamics', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Why Larger Models Learn More: Capacity, Interference, and Rare-Task Retention',
    desc: 'Why do larger models learn tasks smaller models fail on, even with infinite training data? An interactive deep dive into the data-centric mechanics of resource competition, gradient interference, and how scaling acts as a buffer to prevent rare tasks from being overwritten.',
    date: 'June 3, 2026',
    read: postReadTime('why-larger-models-learn-more'),
  },
  {
    href: '/posts/flow-matching-multimodal/',
    tags: [
      { text: 'Flow Matching', cls: 'tag-purple' },
      { text: 'Rectified Flow', cls: 'tag-cyan' },
      { text: 'Multimodal', cls: 'tag-amber' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Flow Matching: The Straight-Line Engine of Modern Multimodal GenAI',
    desc: 'Why did Stable Diffusion 3, Flux.1, and NVIDIA Cosmos ditch traditional diffusion in favor of Flow Matching and Rectified Flow? An interactive deep dive into simulation-free training, Optimal Transport paths, and the mathematics of straight-line vector fields that generate high-quality text, images, and speech in 10 steps.',
    date: 'June 3, 2026',
    read: postReadTime('flow-matching-multimodal'),
  },
  {
    href: '/posts/mtp-speculative-conversion/',
    tags: [
      { text: 'Speculative Decoding', cls: 'tag-purple' },
      { text: 'Multi-Token Prediction', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Recycling MTP: Converting Pre-Trained Multi-Token Prediction Modules into Advanced Speculative Drafters',
    desc: 'Building high-quality draft models is bottlenecked by data scarcity, while native MTP co-training is locked into fixed architectures. Here is how we can recycle pre-trained MTP layers post-hoc into advanced speculative architectures like EAGLE or Medusa, gaining the best of both worlds.',
    date: 'June 3, 2026',
    read: postReadTime('mtp-speculative-conversion'),
  },
  {
    href: '/posts/fixing-multimodal-degradation/',
    tags: [
      { text: 'Multimodal', cls: 'tag-purple' },
      { text: 'Training Dynamics', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'The Modality Regret: How the AI Industry Solved Multimodal Text Degradation',
    desc: 'For years, transforming a highly capable text model into a multimodal model acted like a lobotomy, severely degrading its coding and mathematical reasoning. Here is the engineering history of how we diagnosed the "modality tax" and the architectural breakthroughs that finally fixed it.',
    date: 'June 3, 2026',
    read: postReadTime('fixing-multimodal-degradation'),
  },
  {
    href: '/posts/cosmos3/',
    tags: [
      { text: 'World Models', cls: 'tag-purple' },
      { text: 'Physical AI', cls: 'tag-cyan' },
      { text: 'Multimodal', cls: 'tag-amber' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Cosmos 3: One World Model to Perceive, Reason, Generate, and Act',
    desc: 'NVIDIA\'s Cosmos 3 collapses VLM + video generator + robot policy into a single 64B Mixture-of-Transformers: the same weights answer questions, synthesize video, generate audio, and predict actions — by simply changing which tokens are noisy. #1 open-source on T2I, T2V, I2V, and robot policy leaderboards.',
    date: 'June 2, 2026',
    read: postReadTime('cosmos3'),
  },
  {
    href: '/posts/sdsl-spec-decoding/',
    tags: [
      { text: 'Speculative Decoding', cls: 'tag-purple' },
      { text: 'Scaling Laws', cls: 'tag-cyan' },
      { text: 'LLM Inference', cls: 'tag-amber' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Speculative Decoding Scaling Laws: The 200× Rule',
    desc: 'Before training a single draft model, you can predict the optimal size: it should be ~200× smaller than the target. SDSL derives this analytically — α is an affine function of draft/target perplexity (R²=0.98), and threading pre-training scaling laws through the throughput formula yields N* = 2.71×10⁻³ · M + 87M, validated on OPT, Qwen, and LLaMA families.',
    date: 'June 1, 2026',
    read: postReadTime('sdsl-spec-decoding'),
  },
  {
    href: '/posts/mla-attention/',
    tags: [
      { text: 'Attention', cls: 'tag-purple' },
      { text: 'LLM Inference', cls: 'tag-cyan' },
      { text: 'KV Cache', cls: 'tag-amber' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'MLA: How DeepSeek Shrinks the KV Cache by 93% Without Losing Quality',
    desc: 'Multi-head Latent Attention compresses keys and values into a 576-float latent vector — 57× smaller than standard MHA — using a low-rank projection and a matrix-absorption trick that eliminates key materialization entirely. The result: 5.76× faster generation and better-than-MHA quality in DeepSeek-V2.',
    date: 'June 1, 2026',
    read: postReadTime('mla-attention'),
  },
  {
    href: '/posts/mfu-llm-training/',
    tags: [
      { text: 'LLM Training', cls: 'tag-purple' },
      { text: 'Performance', cls: 'tag-cyan' },
      { text: 'Infrastructure', cls: 'tag-amber' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'MFU: The Number That Tells You How Hard Your GPUs Are Actually Working',
    desc: 'Model FLOPs Utilization answers how much of your GPU\'s 312 TFLOPS you actually use during training. Learn how to calculate it from first principles, what kills it (memory bandwidth, pipeline bubbles, communication), and why 30–70% is the real-world range — with an interactive calculator and benchmarks from PaLM, Megatron-LM, and FlashAttention-2.',
    date: 'June 1, 2026',
    read: postReadTime('mfu-llm-training'),
  },
  {
    href: '/posts/mtp-layernorm-diagnostics/',
    tags: [
      { text: 'MTP Training', cls: 'tag-purple' },
      { text: 'LayerNorm', cls: 'tag-cyan' },
      { text: 'Training Diagnostics', cls: 'tag-amber' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'One Number Tells You Everything: Diagnosing MTP Training with LayerNorm Param-Norms',
    desc: 'When training a Multi-Token Prediction module alongside your LLM, the L₂ norm of final_layernorm.weight starts at √d_model (e.g. 55.4 for d=3072) and tells you whether representations are healthy, over-specialized, or silently corrupting MTP gradient flow. This post shows what that number means, why it drifts, and what to do when it does.',
    date: 'June 1, 2026',
    read: postReadTime('mtp-layernorm-diagnostics'),
  },
  {
    href: '/posts/domino-spec-decoding/',
    tags: [
      { text: 'Speculative Decoding', cls: 'tag-purple' },
      { text: 'LLM Inference', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Domino: Causal Quality at Parallel Speed',
    desc: 'Autoregressive drafters have quality but pay a sequential tax; parallel drafters are fast but miss intra-block causal dependencies. Domino decouples these: a GRU head adds lightweight causal correction on top of a block-parallel drafter, gaining 16.6% acceptance length and 12.3% more speedup with only 2.8% extra latency — reaching 7.92× on GSM8K.',
    date: 'June 1, 2026',
    read: postReadTime('domino-spec-decoding'),
  },
  {
    href: '/posts/esr-early-stopping-rollout/',
    tags: [
      { text: 'Knowledge Distillation', cls: 'tag-purple' },
      { text: 'LLM Training', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Early Stopping Rollout: One Line That Fixes On-Policy Distillation',
    desc: 'On-policy distillation breaks when long student rollouts poison the teacher signal — accuracy falls from 65% to 51% after 300 tokens. ESR fixes it with a single line: stop at N=100 tokens, recover 24× efficiency, and often surpass the teacher entirely.',
    date: 'June 1, 2026',
    read: postReadTime('esr-early-stopping-rollout'),
  },
  {
    href: '/posts/opd-foresight/',
    tags: [
      { text: 'On-Policy Distillation', cls: 'tag-purple' },
      { text: 'LLM Post-Training', cls: 'tag-cyan' },
      { text: 'Mechanistic Analysis', cls: 'tag-amber' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'OPD Has Foresight: Why Distillation Beats RL and How to Exploit It 3×',
    desc: 'On-policy distillation locks onto the correct update direction within 10% of training—far earlier than RL. This foresight manifests as functional redundancy avoidance (OPD ignores useless layers) and early low-rank lock-in (dominant subspaces stabilize fast). EffOPD exploits both for a 3× training speedup with no extra modules.',
    date: 'June 1, 2026',
    read: postReadTime('opd-foresight'),
  },
  {
    href: '/posts/hrm-text/',
    tags: [
      { text: 'Efficient Pretraining', cls: 'tag-purple' },
      { text: 'Recurrent Architecture', cls: 'tag-cyan' },
      { text: 'From Scratch', cls: 'tag-amber' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'HRM-Text: $1,500 to Match Trillion-Token Baselines',
    desc: 'A 1B-parameter hierarchical recurrent model trained from scratch on 40 billion tokens for $1,500 reaches the benchmark neighborhood of 2–7B models trained on 4–36 trillion tokens — using 96–432× less compute. Three co-designed ingredients: dual-timescale HRM recurrence, MagicNorm stabilization, and a response-only PrefixLM training objective.',
    date: 'June 1, 2026',
    read: postReadTime('hrm-text'),
  },
  {
    href: '/posts/opd-local-support-matching/',
    tags: [
      { text: 'On-Policy Distillation', cls: 'tag-purple' },
      { text: 'LLM Post-Training', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'On-Policy Distillation Done Right: Three Failure Modes and One Fix',
    desc: 'Standard on-policy distillation compares just one sampled token per update — imbalanced, drift-prone, and corrupted by tokenizer mismatches. Teacher top-K local support matching fixes all three by comparing distributions over a teacher-selected support set, delivering +19.8% math reasoning gain with no architectural changes.',
    date: 'June 1, 2026',
    read: postReadTime('opd-local-support-matching'),
  },
  {
    href: '/posts/rtpurbo/',
    tags: [
      { text: 'Sparse Attention', cls: 'tag-purple' },
      { text: 'Long Context', cls: 'tag-cyan' },
      { text: 'KV Cache', cls: 'tag-amber' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Full Attention Strikes Back: 9.36× Faster in 600 Steps',
    desc: 'Full-attention LLMs are already intrinsically sparse. RTPurbo identifies the 15% of heads that do real long-range retrieval, routes them through a 16-dimensional indexer, and uses dynamic top-p to adapt token budgets per query — reaching near-lossless accuracy with 9.36× prefill speedup at 1M context in just ~600 training steps.',
    date: 'June 1, 2026',
    read: postReadTime('rtpurbo'),
  },
  {
    href: '/posts/attention-empirical-bayes/',
    tags: [
      { text: 'Theory', cls: 'tag-purple' },
      { text: 'Attention Mechanisms', cls: 'tag-cyan' },
      { text: 'Empirical Bayes', cls: 'tag-amber' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Attention as In-Context Empirical Bayes',
    desc: 'A minimal attention-only transformer implements a principled two-stage empirical Bayes denoiser — depth (Stage 1) refines a particle approximation to the unknown prior via reverse diffusion, while a long-range skip connection (Stage 2) queries it for Bayes-optimal posterior averaging. No noise schedule needed: a fixed bandwidth and T* = σ²/2 layers suffice, with convergence proved for Gaussian priors.',
    date: 'June 1, 2026',
    read: postReadTime('attention-empirical-bayes'),
  },
  {
    href: '/posts/diffusion-opd/',
    tags: [
      { text: 'Diffusion RL', cls: 'tag-purple' },
      { text: 'Multi-Task Learning', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'DiffusionOPD: Multi-Task Alignment via On-Policy Distillation',
    desc: 'Train specialist teachers for aesthetics, OCR, and composition independently, then distill all three into one unified diffusion model along the student\'s own denoising trajectories. A closed-form KL objective replaces PPO\'s noisy estimator, achieving 0.929 average score vs 0.851 for Cascade NFT — in 35% less GPU time.',
    date: 'June 1, 2026',
    read: postReadTime('diffusion-opd'),
  },
  {
    href: '/posts/nvfp4-pretraining/',
    tags: [
      { text: 'Low-Precision Training', cls: 'tag-purple' },
      { text: 'LLM Pretraining', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'FP4 at Scale: NVFP4 Pretraining for LLMs',
    desc: 'NVIDIA trained a 12B Mamba-Transformer on 10 trillion tokens in 4-bit NVFP4 precision—matching FP8 accuracy—using four targeted fixes: mixed precision layers, Hadamard outlier smoothing, 2D weight scaling, and stochastic gradient rounding. On Blackwell GB300 this unlocks 6× the BF16 arithmetic throughput.',
    date: 'May 27, 2026',
    read: postReadTime('nvfp4-pretraining'),
  },
  {
    href: '/posts/tide-dllm/',
    tags: [
      { text: 'Diffusion LLMs', cls: 'tag-purple' },
      { text: 'Knowledge Distillation', cls: 'tag-cyan' },
      { text: 'Cross-Architecture', cls: 'tag-amber' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'TIDE: Shrinking Diffusion LLMs 22× Without Losing the Code Superpower',
    desc: 'Cross-architecture distillation compresses a 16B MoE diffusion LLM into a 0.6B student with 22× less memory and a 16-point HumanEval advantage over same-size AR models. Three synergistic components — TIDAL for noise-adaptive scheduling, CompDemo for richer teacher context, and Reverse CALM for cross-tokenizer alignment — each address a unique barrier that AR distillation never had to solve.',
    date: 'May 27, 2026',
    read: postReadTime('tide-dllm'),
  },
  {
    href: '/posts/replaid/',
    tags: [
      { text: 'Diffusion LMs', cls: 'tag-purple' },
      { text: 'Scaling Laws', cls: 'tag-cyan' },
      { text: 'Continuous Diffusion', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'RePlaid: Continuous Diffusion Scales Competitively with Discrete',
    desc: 'Continuous diffusion was written off after Plaid reported a 64× compute gap vs AR. RePlaid closes it to 20× by aligning the architecture with modern discrete DLMs — and achieves 22.1 PPL on OpenWebText, better than MDLM\'s 23.1. Interactive visualizations show the structured token embedding geometry, the self-correcting noise schedule, and the unified scaling laws.',
    date: 'May 27, 2026',
    read: postReadTime('replaid'),
  },
  {
    href: '/posts/bitlm/',
    tags: [
      { text: 'Diffusion LM', cls: 'tag-purple' },
      { text: 'Tokenization', cls: 'tag-cyan' },
      { text: 'Architecture', cls: 'tag-amber' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'BitLM: Generating Phrases, Not Tokens, with Binary Diffusion',
    desc: 'BitLM replaces the vocabulary softmax with an 18-bit binary code and a diffusion head that jointly denoises a block of 4 tokens at once — making parallel block-level generation native to the model\'s output interface, not a post-hoc trick. Scales smoothly to 8B on FineWeb-350BT with interactive denoising and binary hypercube visualizations.',
    date: 'May 27, 2026',
    read: postReadTime('bitlm'),
  },
  {
    href: '/posts/orthrus/',
    tags: [
      { text: 'Inference', cls: 'tag-purple' },
      { text: 'Diffusion LM', cls: 'tag-cyan' },
      { text: 'Speculative Decoding', cls: 'tag-amber' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Orthrus: Parallel Tokens, Zero Extra Memory',
    desc: 'Orthrus freezes a pre-trained LLM and injects a lightweight diffusion head that shares the exact same KV cache — enabling speculative-decoding-style parallel generation with zero memory overhead and provably lossless output. Up to 7.8× speedup on Qwen3-8B with 0-point accuracy drop on MATH-500.',
    date: 'May 27, 2026',
    read: postReadTime('orthrus'),
  },
  {
    href: '/posts/rrattention/',
    tags: [
      { text: 'Attention', cls: 'tag-purple' },
      { text: 'Long Context', cls: 'tag-cyan' },
      { text: 'Inference Efficiency', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'RRAttention: 2.4× Faster Long-Context Inference via Round-Robin Head Sampling',
    desc: 'Every dynamic sparse attention method trades off some desirable property to gain another — global coverage requires mixing queries, query independence requires local scope. RRAttention breaks this deadlock by rotating which position each head samples within a stride, recovering 99.7% of full-attention accuracy while computing only half the blocks.',
    date: 'May 27, 2026',
    read: postReadTime('rrattention'),
  },
  {
    href: '/posts/llm-benchmark-bias/',
    tags: [
      { text: 'LLM Inference', cls: 'tag-purple' },
      { text: 'Benchmarking', cls: 'tag-cyan' },
      { text: 'Systems', cls: 'tag-amber' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Your LLM Benchmark Is Lying to You',
    desc: 'At 1,000 QPS, single-process asyncio tools achieve only 14–44% of their target request rate and inject up to 58 seconds of fake latency — all due to Python\'s GIL. A Google paper models this with M/G/1 queueing theory, formalizes the NTPOT metric, and fixes it with a multi-process architecture that cuts artificial overhead to 0.63ms.',
    date: 'May 27, 2026',
    read: postReadTime('llm-benchmark-bias'),
  },
  {
    href: '/posts/sparse-to-dense-reward/',
    tags: [
      { text: 'LLM Post-Training', cls: 'tag-purple' },
      { text: 'Distillation', cls: 'tag-cyan' },
      { text: 'RL', cls: 'tag-amber' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Sparse-to-Dense Reward: Why Your Labeled Data Belongs on the Teacher',
    desc: 'Spending scarce verifiable training data on the small deployment model is a misallocation. Run RL on the teacher first, then bridge to the student with dense FKL-to-OPD supervision. A raw 14× larger teacher distilled without Stage 1 is worse than cold GRPO — scale alone doesn\'t satisfy the reward-shaping condition.',
    date: 'May 27, 2026',
    read: postReadTime('sparse-to-dense-reward'),
  },
  {
    href: '/posts/opd-spec-decoding/',
    tags: [
      { text: 'Speculative Decoding', cls: 'tag-purple' },
      { text: 'On-Policy Distillation', cls: 'tag-cyan' },
      { text: 'Ideas + Analysis', cls: 'tag-amber' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'On-Policy Training for Speculative Decoding: Disaggregated Rollout-Verify-Train',
    desc: 'Draft models have the same exposure bias as KD students — they train on clean target sequences but deploy into a mix of accepted and rejected tokens. On-policy training through the full accept/reject loop fixes this. A disaggregated RVT architecture — borrowing prefill/decode separation from serving infra — makes it scale.',
    date: 'May 27, 2026',
    read: postReadTime('opd-spec-decoding'),
  },
  {
    href: '/posts/on-policy-distillation/',
    tags: [
      { text: 'Knowledge Distillation', cls: 'tag-purple' },
      { text: 'LLM Training', cls: 'tag-cyan' },
      { text: 'Survey', cls: 'tag-amber' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'On-Policy Distillation: A 2026 Field Guide',
    desc: 'Exposure bias silently breaks knowledge distillation for long sequences. On-policy distillation fixes it — but which divergence objective, and can RL do better? An interactive survey mapping GKD, MiniLLM, DistiLLM, STILL, rStar-Math, and the 2025–2026 frontier including the RL-vs-distillation debate.',
    date: 'May 27, 2026',
    read: postReadTime('on-policy-distillation'),
  },
  {
    href: '/posts/validate-mtp-ideas/',
    tags: [
      { text: 'Multi-Token Prediction', cls: 'tag-purple' },
      { text: 'LLM Training', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'How to Validate a Multi-Token Prediction Idea Before Burning a Pre-Training Run',
    desc: 'Training a frontier model costs months and millions. Four targeted steps — proxy training on polynomial tasks, loss gap monitoring over 5K steps, speculative decoding scaling law estimation, and Little\'s Law serving simulation — de-risk a novel MTP architecture in hours using a 30M–100M sandbox.',
    date: 'May 27, 2026',
    read: postReadTime('validate-mtp-ideas'),
  },
  {
    href: '/posts/longcat-flash/',
    tags: [
      { text: 'MoE Architecture', cls: 'tag-purple' },
      { text: 'Dynamic Compute', cls: 'tag-cyan' },
      { text: 'LLM Training', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'LongCat-Flash: 560B MoE with Dynamic Compute per Token',
    desc: 'Meituan\'s LongCat-Flash adds zero-computation experts to MoE routing — identity functions that cost nothing — so easy tokens ("the", "and") skip FFN compute while hard tokens ("eigenvector", "backpropagation") get more. Combined with Shortcut-Connected MoE that halves inference TPOT to 16ms, the result is a 560B model that trains 20T tokens in 30 days and serves at 100+ TPS for $0.70/M output tokens.',
    date: 'May 27, 2026',
    read: postReadTime('longcat-flash'),
  },
  {
    href: '/posts/representation-autoencoders-v2/',
    tags: [
      { text: 'Diffusion Models', cls: 'tag-purple' },
      { text: 'Image Generation', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'RAEv2: 10× Faster Image Generation with Smarter Encoder Layers',
    desc: 'Three elegant insights — summing the last K encoder layers for a free reconstruction-generation trade-off, combining RAE with REPA (they\'re complementary, not redundant), and reusing the REPA head for internal guidance with no extra model or forward pass — cut diffusion training from 800 to 80 epochs while setting a new state-of-the-art gFID of 1.06.',
    date: 'May 26, 2026',
    read: postReadTime('representation-autoencoders-v2'),
  },
  {
    href: '/posts/bitter-lesson-data-filtering/',
    tags: [
      { text: 'Data Curation', cls: 'tag-purple' },
      { text: 'Scaling Laws', cls: 'tag-cyan' },
      { text: 'Pretraining', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'A Bitter Lesson for Data Filtering',
    desc: 'Filtering training data to keep only "high-quality" content is almost universally assumed to be necessary. Stanford researchers challenge this: with enough compute and large enough models, unfiltered Common Crawl outperforms every standard filter — and models even benefit from shuffled-word junk. Interactive crossover charts show exactly when and why the ranking flips.',
    date: 'May 26, 2026',
    read: postReadTime('bitter-lesson-data-filtering'),
  },
  {
    href: '/posts/dynamo-llm/',
    tags: [
      { text: 'LLM Serving', cls: 'tag-purple' },
      { text: 'Energy Efficiency', cls: 'tag-cyan' },
      { text: 'Systems', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'DynamoLLM: Cutting 53% of LLM Serving Energy Without Touching Accuracy',
    desc: 'Current LLM clusters run every request — from "Hello!" to a 4,000-token code review — at TP8 max-frequency, wasting 3× energy on short requests. DynamoLLM routes requests to per-type pools with tuned tensor parallelism and GPU frequency, saving 53% energy, 38% CO₂, and 61% cost while meeting all latency SLOs. Interactive energy landscape shows how the optimal config shifts completely across request types.',
    date: 'May 26, 2026',
    read: postReadTime('dynamo-llm'),
  },
  {
    href: '/posts/opd-failure-modes/',
    tags: [
      { text: 'LLM Training', cls: 'tag-purple' },
      { text: 'Distillation', cls: 'tag-cyan' },
      { text: 'RLVR', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'The Many Faces of On-Policy Distillation: Pitfalls, Mechanisms, and Fixes',
    desc: 'On-policy distillation can catastrophically collapse or silently fail depending on three subtle traps: prefix mismatch (teacher accuracy drops 62%→46%), biased TopK gradients (causes training collapse), and a structural OPSD limitation where instance-specific PI averages to a weak policy. Interactive visualizations show each failure mode and the fixes that resolve them.',
    date: 'May 26, 2026',
    read: postReadTime('opd-failure-modes'),
  },
  {
    href: '/posts/fast-blt/',
    tags: [
      { text: 'Byte-Level LMs', cls: 'tag-purple' },
      { text: 'Diffusion Generation', cls: 'tag-cyan' },
      { text: 'Speculative Decoding', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Fast Byte Latent Transformer: Generating Multiple Bytes at Once',
    desc: 'BLT-D adds block-wise diffusion to byte-level language models — instead of one byte at a time, the decoder unmasks an entire block in parallel, cutting memory bandwidth by up to 92%. BLT-S and BLT-DV add speculative verification for lossless or near-lossless quality at 52–77% lower bandwidth.',
    date: 'May 26, 2026',
    read: postReadTime('fast-blt'),
  },
  {
    href: '/posts/gated-deltanet-2/',
    tags: [
      { text: 'Linear Attention', cls: 'tag-purple' },
      { text: 'Recurrent LLMs', cls: 'tag-cyan' },
      { text: 'Delta Rule', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Gated DeltaNet-2: Untying Erase and Write in Linear Attention',
    desc: 'Every delta-rule recurrent model (DeltaNet, KDA) uses a single scalar to control both erasing stale memory and writing new content. Gated DeltaNet-2 (NVIDIA, 2026) splits these into independent channel-wise gates, improving multi-key retrieval accuracy by 35% over KDA at 4K context while preserving efficient chunkwise training.',
    date: 'May 26, 2026',
    read: postReadTime('gated-deltanet-2'),
  },
  {
    href: '/posts/oscar-int2-kv-cache/',
    tags: [
      { text: 'Quantization', cls: 'tag-purple' },
      { text: 'LLM Serving', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'OSCAR: 2-bit KV Cache Without the Accuracy Cliff',
    desc: 'How attention-aware covariance rotations make INT2 KV-cache quantization both accurate and deployable — closing the BF16 accuracy gap to 3.78 points on Qwen3 while delivering 8× memory reduction and 3–7× throughput gains in production SGLang serving.',
    date: 'May 26, 2025',
    read: postReadTime('oscar-int2-kv-cache'),
  },
  {
    href: '/posts/cola-dlm/',
    tags: [
      { text: 'Diffusion Language Models', cls: 'tag-purple' },
      { text: 'Continuous Latent Space', cls: 'tag-cyan' },
      { text: 'Flow Matching', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'Cola DLM: When Diffusion Stops Recovering Tokens and Starts Thinking in Latents',
    desc: 'Cola DLM (ByteDance Seed, 2026) separates text generation into global semantic organization — a block-causal DiT running flow matching over compressed latent blocks — and local token realization via a conditional decoder. It outperforms matched 2B AR and LLaDA baselines at 2000 EFLOPs on reasoning tasks, while exposing a fundamental PPL-vs-generation mismatch in continuous latent models.',
    date: 'May 26, 2026',
    read: postReadTime('cola-dlm'),
  },
  {
    href: '/posts/ptrm/',
    tags: [
      { text: 'Tiny Recursive Model', cls: 'tag-purple' },
      { text: 'Test-Time Compute', cls: 'tag-cyan' },
      { text: 'Latent-Space Reasoning', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'A 7M-Param Model That Beats GPT-5 at Reasoning Puzzles',
    desc: 'PTRM (Probabilistic Tiny Recursive Model) injects Gaussian noise into a 7M-parameter model\'s hidden state, runs K parallel guesses, and lets the model pick its own best answer — scoring 91.2% on pencil puzzles vs 55.1% for a 7-LLM ensemble, at 0.0001× the cost. A visual deep-dive into latent basins, width vs depth scaling, and the Q-head trick.',
    date: 'May 26, 2026',
    read: postReadTime('ptrm'),
  },
  {
    href: '/posts/elf-continuous-dlm/',
    tags: [
      { text: 'Diffusion Language Models', cls: 'tag-purple' },
      { text: 'Flow Matching', cls: 'tag-cyan' },
      { text: 'Continuous Embeddings', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: 'ELF: Continuous Diffusion for Language Finally Works',
    desc: 'How Embedded Language Flows (MIT, 2026) beats leading discrete diffusion models using 10× fewer training tokens — by staying in continuous embedding space for the entire denoising trajectory and discretizing only at the final step. With animated flow visualizations and pseudocode walkthroughs.',
    date: 'May 26, 2026',
    read: postReadTime('elf-continuous-dlm'),
  },
  {
    href: '/posts/speculative-decoding-compare/',
    tags: [
      { text: 'Speculative Decoding', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
      { text: 'Comparison', cls: 'tag-purple' },
    ],
    title: 'Speculative Decoding Methods Compared',
    desc: 'Side-by-side comparison of six speculative decoding systems — EAGLE-3, DeepSeek MTP, DFlash, P-EAGLE, DART, and Gemma 4 MTP. Pick any two to compare: backbone access signal, draft architecture, inference cost, and core tradeoffs.',
    date: 'May 26, 2026',
    read: postReadTime('speculative-decoding-compare'),
  },
  {
    href: '/posts/gemma4-mtp/',
    tags: [
      { text: 'Gemma 4', cls: 'tag-purple' },
      { text: 'Multi-Token Prediction', cls: 'tag-cyan' },
      { text: 'Speculative Decoding', cls: 'tag-cyan' },
      { text: 'Interactive', cls: 'tag-green' },
    ],
    title: "How Gemma 4's Multi-Token Prediction Works",
    desc: 'A deep dive into the Gemma4 Assistant architecture: KV sharing between the backbone and draft model, centroid-based vocabulary prediction, and bidirectional attention masks — with animated visualizations and annotated source code.',
    date: 'May 24, 2026',
    read: postReadTime('gemma4-mtp'),
  },
];

const initialPosts = latestPosts.slice(0, BATCH);
const deferredPosts = latestPosts.slice(BATCH);
---
<BaseLayout title="AI/ML Deep Dives">
<style is:global>
  .hero {
    padding: 6.5rem 2rem 4rem;
    max-width: 1040px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 340px;
    gap: 3rem;
    align-items: center;
  }
  .hero-text { display: flex; flex-direction: column; }
  .hero-eyebrow {
    display: inline-flex;
    align-items: center;
    gap: .625rem;
    font-size: .72rem;
    font-weight: 600;
    letter-spacing: .1em;
    text-transform: uppercase;
    color: var(--purple-l);
    margin-bottom: 1.5rem;
    font-family: var(--ff-mono);
  }
  .hero-eyebrow::before {
    content: '';
    width: 18px;
    height: 1px;
    background: var(--purple);
  }
  .hero-text h1 {
    font-size: clamp(2rem, 5vw, 3.4rem);
    font-weight: 800;
    letter-spacing: -.03em;
    line-height: 1.1;
    margin-bottom: 1.25rem;
    background: linear-gradient(160deg, var(--text) 40%, var(--muted2));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
  .hero-text p {
    font-size: 1.075rem;
    color: var(--muted2);
    max-width: 560px;
    line-height: 1.75;
  }
  /* Viz */
  .viz-panel {
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 1.25rem;
    user-select: none;
  }
  .viz-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1rem;
  }
  .viz-label { font-family: var(--ff-mono); font-size: .72rem; color: var(--purple-l); }
  .viz-step  { font-family: var(--ff-mono); font-size: .68rem; color: var(--muted); }
  .viz-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 6px;
  }
  .viz-token {
    position: relative;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 6px;
    height: 38px;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    transition: border-color .4s;
  }
  .viz-mask {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: opacity .35s ease;
  }
  .viz-mask::after {
    content: '';
    display: block;
    width: 56%;
    height: 7px;
    background: var(--border-hi);
    border-radius: 3px;
    animation: viz-shimmer 1.8s ease-in-out infinite;
  }
  @keyframes viz-shimmer {
    0%, 100% { opacity: .4; }
    50%       { opacity: .85; }
  }
  .viz-word {
    font-family: var(--ff-mono);
    font-size: .67rem;
    color: var(--purple-l);
    opacity: 0;
    transition: opacity .35s ease;
    position: relative;
    z-index: 1;
    white-space: nowrap;
  }
  .viz-token.revealed               { border-color: rgba(139,92,246,.38); }
  .viz-token.revealed .viz-mask     { opacity: 0; }
  .viz-token.revealed .viz-word     { opacity: 1; }
  .viz-token.flash                  { animation: viz-flash .4s ease; }
  @keyframes viz-flash {
    0%   { background: rgba(139,92,246,.22); }
    100% { background: var(--surface); }
  }
  .viz-footer { margin-top: 1rem; }
  .viz-progress { height: 2px; background: var(--border); border-radius: 1px; overflow: hidden; }
  .viz-fill {
    height: 100%;
    background: linear-gradient(90deg, var(--purple), var(--cyan));
    border-radius: 1px;
    width: 0%;
    transition: width .2s ease;
  }
  @media (max-width: 900px) {
    .hero { grid-template-columns: 1fr; }
    .hero-viz { display: none; }
  }
  @media (max-width: 640px) {
    .hero { padding: 5rem 1.25rem 3rem; }
    .posts-section { padding: 0 1.25rem 5rem; }
    .post-card { padding: 1.25rem; }
    .post-card.featured { padding: 1.5rem 1.25rem; }
    .post-card h3 { font-size: 1.05rem; }
    .post-card.featured h3 { font-size: 1.2rem; }
    .post-meta { flex-wrap: wrap; gap: .5rem; }
  }
  .posts-section {
    max-width: 860px;
    margin: 0 auto;
    padding: 0 2rem 7rem;
  }
  .section-rule {
    display: flex;
    align-items: center;
    gap: .875rem;
    margin-bottom: 2rem;
  }
  .section-rule h2 {
    font-size: .72rem;
    font-weight: 600;
    letter-spacing: .1em;
    text-transform: uppercase;
    color: var(--muted);
    white-space: nowrap;
    font-family: var(--ff-mono);
  }
  .section-rule::after {
    content: '';
    flex: 1;
    height: 1px;
    background: var(--border);
  }
  .post-card {
    display: block;
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 1.75rem 2rem;
    text-decoration: none;
    transition: border-color .2s, transform .2s, box-shadow .2s;
    margin-bottom: 1rem;
    position: relative;
    overflow: hidden;
  }
  .post-card::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, var(--purple-dim) 0%, transparent 55%);
    opacity: 0;
    transition: opacity .3s;
    pointer-events: none;
  }
  .post-card:hover {
    border-color: rgba(139,92,246,.5);
    transform: translateY(-2px);
    box-shadow: 0 8px 40px rgba(139,92,246,.1);
    text-decoration: none;
  }
  .post-card:hover::before { opacity: 1; }
  .post-featured-badge {
    display: inline-flex;
    align-items: center;
    gap: .3rem;
    font-size: .68rem;
    font-weight: 600;
    letter-spacing: .07em;
    text-transform: uppercase;
    color: var(--amber);
    background: rgba(245,158,11,.09);
    border: 1px solid rgba(245,158,11,.22);
    border-radius: 4px;
    padding: .18rem .55rem;
    margin-bottom: .875rem;
    font-family: var(--ff-mono);
  }
  .post-card .tags { margin-bottom: .875rem; }
  .post-card h3 {
    font-size: 1.2rem;
    font-weight: 700;
    letter-spacing: -.02em;
    color: var(--text);
    margin-bottom: .5rem;
    line-height: 1.3;
  }
  .post-card p {
    font-size: .875rem;
    color: var(--muted2);
    line-height: 1.65;
    margin-bottom: 1.25rem;
  }
  .post-meta {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    font-size: .775rem;
    color: var(--muted);
  }
  .post-arrow {
    margin-left: auto;
    color: var(--purple-l);
    font-size: 1.1rem;
    transition: transform .2s;
  }
  .post-card:hover .post-arrow { transform: translateX(4px); }
  .post-card.featured {
    border-color: rgba(139,92,246,.35);
    background: linear-gradient(135deg, rgba(139,92,246,.06) 0%, var(--card) 60%);
    padding: 2.25rem 2.5rem;
  }
  .post-card.featured h3 {
    font-size: 1.45rem;
  }
  .post-card.featured p {
    font-size: .9rem;
    max-width: 680px;
  }
  #posts-sentinel {
    height: 1px;
  }
  /* ── Search ── */
  .search-wrap {
    position: relative;
    margin-bottom: 1.25rem;
  }
  .search-icon {
    position: absolute;
    left: .875rem;
    top: 50%;
    transform: translateY(-50%);
    width: 15px;
    height: 15px;
    color: var(--muted);
    pointer-events: none;
  }
  .search-input {
    width: 100%;
    box-sizing: border-box;
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    color: var(--text);
    font-size: .875rem;
    font-family: var(--ff-sans, inherit);
    padding: .7rem .9rem .7rem 2.5rem;
    outline: none;
    transition: border-color .2s, box-shadow .2s;
    -webkit-appearance: none;
  }
  .search-input::placeholder { color: var(--muted); }
  .search-input:focus {
    border-color: rgba(139,92,246,.5);
    box-shadow: 0 0 0 3px rgba(139,92,246,.1);
  }
  .search-input::-webkit-search-cancel-button { -webkit-appearance: none; }
  .search-status {
    font-family: var(--ff-mono);
    font-size: .72rem;
    color: var(--muted);
    margin-bottom: 1rem;
  }
  .search-empty {
    text-align: center;
    padding: 3.5rem 0;
    color: var(--muted);
    font-size: .9rem;
    line-height: 1.7;
  }
  .search-empty strong { color: var(--text2); }
  .post-progress-bar {
    position: absolute;
    bottom: 0;
    left: 0;
    height: 3px;
    background: linear-gradient(90deg, var(--purple), var(--purple-l));
    border-radius: 0 2px 0 0;
    pointer-events: none;
  }
  footer {
    border-top: 1px solid var(--border);
    padding: 1.75rem 2rem;
    text-align: center;
    font-size: .775rem;
    color: var(--muted);
  }
</style>

<section class="hero">
  <div class="hero-text">
    <div class="hero-eyebrow" data-i18n="hero-eyebrow">Personal Research Blog</div>
    <h1 data-i18n="hero-title">Exploring the frontier of AI &amp; ML</h1>
    <p data-i18n="hero-desc">Deep dives into research papers, model architectures, and open-source implementations — with interactive visualizations and annotated source code.</p>
  </div>
  <div class="hero-viz" id="hero-viz" aria-hidden="true"></div>
</section>

<section class="posts-section">
  <div class="section-rule"><h2>Featured</h2></div>

  <a class="post-card featured" href="/posts/gemma4-mtp/">
    <div class="post-featured-badge">★ Featured</div>
    <div class="tags">
      <span class="tag tag-purple">Gemma 4</span>
      <span class="tag tag-cyan">Multi-Token Prediction</span>
      <span class="tag tag-cyan">Speculative Decoding</span>
      <span class="tag tag-green">Interactive</span>
    </div>
    <h3>How Gemma 4's Multi-Token Prediction Works</h3>
    <p>A deep dive into the Gemma4 Assistant architecture: KV sharing between the backbone and draft model, centroid-based vocabulary prediction, and bidirectional attention masks — with animated visualizations and annotated source code.</p>
    <div class="post-meta">
      <span>May 24, 2026</span>
      <span data-i18n="post-read">{postReadTime('gemma4-mtp')}</span>
      <span class="post-arrow">→</span>
    </div>
  </a>

  <div class="section-rule" style="margin-top:2.5rem"><h2 data-i18n="section-latest">Latest Posts</h2></div>

  <div class="search-wrap">
    <svg class="search-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
      <circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/>
    </svg>
    <input id="post-search" class="search-input" type="search"
      placeholder="Search posts by title, topic, or tag…"
      autocomplete="off" spellcheck="false" aria-label="Search posts" />
  </div>
  <div id="search-status" class="search-status" hidden></div>

  <div id="posts-list">
    {initialPosts.map(post => (
      <a class="post-card" href={post.href}>
        {post.badge && <div class="post-featured-badge" data-i18n="post-badge">{post.badge}</div>}
        <div class="tags">
          {post.tags.map(t => (
            <span class={`tag ${t.cls}`}>{t.text}</span>
          ))}
        </div>
        <h3>{post.title}</h3>
        <p>{post.desc}</p>
        <div class="post-meta">
          <span>{post.date}</span>
          <span data-i18n="post-read">{post.read}</span>
          <span class="post-arrow">→</span>
        </div>
      </a>
    ))}
    <div id="posts-sentinel"></div>
  </div>
</section>

{deferredPosts.length > 0 && (
  <script type="application/json" id="deferred-posts" set:html={JSON.stringify(deferredPosts)} />
)}
<script type="application/json" id="all-posts" set:html={JSON.stringify(latestPosts)} />

<footer data-i18n="blog-footer">Built with curiosity. Source on GitHub.</footer>
</BaseLayout>

<script>
  import { initViz } from '../lib/viz';
  initViz();
</script>

<script>
  const safeGetItem = (key: string, fallback: string = ''): string => {
    try {
      return localStorage.getItem(key) || fallback;
    } catch (e) {
      return fallback;
    }
  };

  function applyReadProgress() {
    document.querySelectorAll<HTMLAnchorElement>('.post-card[href]').forEach(card => {
      if (card.querySelector('.post-progress-bar')) return;
      const slug = card.getAttribute('href')?.match(/\/posts\/([^/]+)/)?.[1];
      if (!slug) return;
      const pct = parseInt(safeGetItem(`heji-read-${slug}`, '0') ?? '0', 10);
      if (pct <= 0) return;
      const el = document.createElement('div');
      el.className = 'post-progress-bar';
      el.style.width = `${pct}%`;
      card.appendChild(el);
    });
  }

  applyReadProgress();

  const sentinel = document.getElementById('posts-sentinel');
  const dataEl   = document.getElementById('deferred-posts');
  if (!sentinel || !dataEl) {
    // nothing to lazy-load
  } else {
    const CHUNK = 20;
    const posts: Array<{
      href: string;
      badge?: string;
      tags: { text: string; cls: string }[];
      title: string;
      desc: string;
      date: string;
      read: string;
    }> = JSON.parse(dataEl.textContent!);
    const list = document.getElementById('posts-list')!;
    let idx = 0;

    function esc(s: string) {
      return s.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;');
    }

    function buildCard(p: typeof posts[0]): string {
      const badge = p.badge
        ? `<div class="post-featured-badge" data-i18n="post-badge">${esc(p.badge)}</div>`
        : '';
      const tags = p.tags
        .map(t => `<span class="tag ${esc(t.cls)}">${esc(t.text)}</span>`)
        .join('');
      return `<a class="post-card" href="${esc(p.href)}">
        ${badge}
        <div class="tags">${tags}</div>
        <h3>${esc(p.title)}</h3>
        <p>${esc(p.desc)}</p>
        <div class="post-meta">
          <span>${esc(p.date)}</span>
          <span data-i18n="post-read">${esc(p.read)}</span>
          <span class="post-arrow">→</span>
        </div>
      </a>`;
    }

    function applyCurrentLang(container: HTMLElement) {
      const lang = safeGetItem('heji-lang', 'en') || 'en';
      if (lang !== 'zh') return;
      const zhRead = '~15 分钟阅读';
      const zhBadge = '★ 最新';
      container.querySelectorAll<HTMLElement>('[data-i18n="post-read"]').forEach(el => {
        el.textContent = zhRead;
      });
      container.querySelectorAll<HTMLElement>('[data-i18n="post-badge"]').forEach(el => {
        el.textContent = zhBadge;
      });
    }

    const observer = new IntersectionObserver(entries => {
      if (!entries[0].isIntersecting) return;
      const chunk = posts.slice(idx, idx + CHUNK);
      if (chunk.length === 0) { observer.disconnect(); return; }
      idx += chunk.length;

      const tmp = document.createElement('div');
      tmp.innerHTML = chunk.map(buildCard).join('');
      applyCurrentLang(tmp);
      const frag = document.createDocumentFragment();
      while (tmp.firstChild) frag.appendChild(tmp.firstChild);
      list.insertBefore(frag, sentinel);
      applyReadProgress();

      if (idx >= posts.length) observer.disconnect();
    }, { rootMargin: '300px' });

    observer.observe(sentinel);
  }
</script>

<script>
  const safeGetItem = (key: string, fallback: string = ''): string => {
    try {
      return localStorage.getItem(key) || fallback;
    } catch (e) {
      return fallback;
    }
  };

  // ── Client-side instant search ──────────────────────────────────────────
  interface SearchPost {
    href: string;
    badge?: string;
    tags: { text: string; cls: string }[];
    title: string;
    desc: string;
    date: string;
    read: string;
  }

  const searchInput  = document.getElementById('post-search')   as HTMLInputElement | null;
  const searchStatus = document.getElementById('search-status')  as HTMLElement | null;
  const allPostsEl   = document.getElementById('all-posts');
  const listEl       = document.getElementById('posts-list');

  if (searchInput && allPostsEl && listEl) {
    const allPosts: SearchPost[] = JSON.parse(allPostsEl.textContent!);

    // Insert a dedicated results container before the lazy-load sentinel so
    // deferred cards are never interleaved with search results.
    const sentinelEl = document.getElementById('posts-sentinel');
    const resultsEl  = document.createElement('div');
    resultsEl.id = 'search-results';
    listEl.insertBefore(resultsEl, sentinelEl);

    function escSearch(s: string) {
      return s
        .replace(/&/g, '&amp;').replace(/</g, '&lt;')
        .replace(/>/g, '&gt;').replace(/"/g, '&quot;');
    }

    function buildSearchCard(p: SearchPost): string {
      const badge = p.badge
        ? `<div class="post-featured-badge">${escSearch(p.badge)}</div>`
        : '';
      const tags = p.tags
        .map(t => `<span class="tag ${escSearch(t.cls)}">${escSearch(t.text)}</span>`)
        .join('');
      return `<a class="post-card" href="${escSearch(p.href)}">
        ${badge}
        <div class="tags">${tags}</div>
        <h3>${escSearch(p.title)}</h3>
        <p>${escSearch(p.desc)}</p>
        <div class="post-meta">
          <span>${escSearch(p.date)}</span>
          <span>${escSearch(p.read)}</span>
          <span class="post-arrow">→</span>
        </div>
      </a>`;
    }

    function applyReadProgressSearch(container: HTMLElement) {
      container.querySelectorAll<HTMLAnchorElement>('.post-card[href]').forEach(card => {
        if (card.querySelector('.post-progress-bar')) return;
        const slug = card.getAttribute('href')?.match(/\/posts\/([^/]+)/)?.[1];
        if (!slug) return;
        const pct = parseInt(safeGetItem(`heji-read-${slug}`, '0') ?? '0', 10);
        if (pct <= 0) return;
        const el = document.createElement('div');
        el.className = 'post-progress-bar';
        el.style.width = `${pct}%`;
        card.appendChild(el);
      });
    }

    // Toggle visibility of the statically-rendered initial cards + sentinel
    function setSearchMode(active: boolean) {
      listEl!.querySelectorAll<HTMLElement>(':scope > .post-card').forEach(c => {
        c.style.display = active ? 'none' : '';
      });
      if (sentinelEl) sentinelEl.style.display = active ? 'none' : '';
    }

    searchInput.addEventListener('input', () => {
      const raw = searchInput.value;
      const q   = raw.trim().toLowerCase();

      if (!q) {
        setSearchMode(false);
        resultsEl.innerHTML = '';
        if (searchStatus) searchStatus.hidden = true;
        return;
      }

      setSearchMode(true);

      const words   = q.split(/\s+/).filter(Boolean);
      const matches = allPosts.filter(p => {
        const hay = [p.title, p.desc, ...p.tags.map(t => t.text)]
          .join(' ').toLowerCase();
        return words.every(w => hay.includes(w));
      });

      if (matches.length === 0) {
        resultsEl.innerHTML =
          `<div class="search-empty">No posts found for <strong>"${escSearch(raw)}"</strong></div>`;
      } else {
        resultsEl.innerHTML = matches.map(buildSearchCard).join('');
        applyReadProgressSearch(resultsEl);
      }

      if (searchStatus) {
        searchStatus.hidden      = false;
        searchStatus.textContent = matches.length === 1
          ? '1 result'
          : `${matches.length} results`;
      }
    });
  }
</script>
